extends layout

block content
  h1= title
  p Welcome to #{title}
  ul
    each message in messages
      li #{message.text}
        ul
          li #{message.user}, #{message.added}
      br

  a(href="/new") New Message